#pragma once
#include <iostream>
#include <string>


// Thought it would work didn't no clue why
class ConsoleCF
{
public:
	template <typename T>

	void Print(T t);

	template <typename T>

	void Read(T t);
};
